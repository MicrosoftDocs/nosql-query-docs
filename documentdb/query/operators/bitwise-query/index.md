---
title: Bitwise Query - Operators
description: Bitwise query operators are used to query and filter based on bitwise operations.
type: operators
category: bitwise-query
---

# Bitwise query - operators

Bitwise query operators are used to query and filter based on bitwise operations.

| | Description |
| --- | --- |
| [**`$bitsAllClear`**]($bitsAllClear.md) | The $bitsAllClear operator is used to match documents where all the bit positions specified in a bitmask are clear. |
| [**`$bitsAllSet`**]($bitsAllSet.md) | The bitsAllSet command is used to match documents where all the specified bit positions are set. |
| [**`$bitsAnyClear`**]($bitsAnyClear.md) | The $bitsAnyClear operator matches documents where any of the specified bit positions in a bitmask are clear. |
| [**`$bitsAnySet`**]($bitsAnySet.md) | The $bitsAnySet operator returns documents where any of the specified bit positions are set to 1. |
